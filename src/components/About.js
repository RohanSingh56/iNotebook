import React from 'react'
// import noteContext from '../context/notes/noteContext'

const About = () => {
  
  return (
    <div>
        <h2>About this website</h2>
        <p>1.This website primarily focuses on using MERN stack to store notes in databases, to work with the backend and perform CRUD operations on those notes. 
        <br />
        2. For those operations, we're building our own API. <br />
        3. It doesn't concentrate on the website's CSS.</p>
    </div>
  )
}

export default About